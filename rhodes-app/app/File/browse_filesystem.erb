<div data-role="page">

  <%= render :partial => "/partials/page_head" %>
    
  <div data-role="content">
  
    <%= render :partial => "/partials/example_header", :locals => {:title => "Browse filesystem", :description => "
      View all folders and files in the application bundle
      
      
    " }
    %>

    <%= render :partial => "/partials/language_tabs" %>
    
    <div class="sample ruby">
      Folders:
      <ul>
      <!-- TODO: move from RhoFile to File when implemented -->
        <li><a href="<%= url_for :action=> :browse_filesystem, :query => {:start_from_path => Rho::RhoFile.dirname(@start_from_path)} %>">[One level up]</a></li>      
        <% @folders.each do |folder| %>
            <li><a href="<%= url_for :action=> :browse_filesystem, :query => {:start_from_path => Rho::RhoFile.join(@start_from_path,folder)} %>"><%= folder %></a></li> 
        <% end %>
      </ul>
        
      Files:
      <ul>
        <% @files.each do |file| %>
          <li><%= file %></li>
        <% end %>
      </ul>
    </div>
    
    <div class="sample javascript">
      <ul class="folders">
      </ul>
      
      <ul class="files">
      </ul>
    </div>
    
    
  </div>
